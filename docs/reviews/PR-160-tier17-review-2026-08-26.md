# PR #160 review — Tier 17 cadence recalibration

Reviewed: 2026-08-26

Verdict: technically sound; awaiting explicit human approval for the
`NORM_BOUNDS` change before landing.

The estimator changes address real unit/measurement defects: robust IKI
dispersion replaces pause-dominated raw CV, burst ratio becomes speed-invariant,
missing observations map to each feature's raw midpoint, percentage deletion
rates are normalized, and implausible timing artifacts fail neutral. The branch
keeps Tier 17 disabled and clearly labels transcription-population anchors as
guardrails rather than student denominators.

Evidence checked:

- Read the complete one-commit diff (`369514d2`), including the calibration
  provenance and all estimator/bounds changes.
- Ran `tests/test_tier17_features.py tests/test_readiness.py`: **31 passed**.
- Confirmed the PR remains open and changes three `NORM_BOUNDS` entries.

No code-level blocker was found. The repository rules nevertheless require
explicit human permission for any `NORM_BOUNDS` change, so this campaign does
not cherry-pick or merge PR #160. Once approved, rerun the full suite before
merge and retain the documented requirement to re-derive guardrail bounds from
real proctored pilot data before enabling Tier 17.
