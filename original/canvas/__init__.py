"""
canvas/ — Canvas LTI 1.3 and Document Processor integration package.

Provides:
  keys.py      — RSA-256 key pair management (JWKS endpoint)
  lti.py       — LTI 1.3 OIDC login, launch, and Deep Linking routes
  webhook.py   — Canvas submission event receiver (Plagiarism Framework + Document Processor)
  reporter.py  — Post originality/integrity reports back to Canvas SpeedGrader
"""
