# Alembic Configuration

[alembic]

# path to migration scripts
script_location = alembic

# template for creating migration file
file_template = %%(rev)s_%%(slug)s

# sys.path path, will be prepended to sys.path so `original` imports resolve
# when alembic is invoked from the repo root without an installed package.
prepend_sys_path = .

# timezone to use when rendering the date within the migration file
# and filenames.  Leave blank for localtime.
# timezone =

# Fallback only — the DATABASE_URL env var takes precedence (see alembic/env.py).
sqlalchemy.url = postgresql://original:original@localhost:5432/original_db

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARN
handlers = console
qualname =

[logger_sqlalchemy]
level = WARN
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
