# fastapi 0.115.0 required starlette<0.39.0, incompatible with the starlette>=0.47.2
# security pin below (CVE-2025-54121). fastapi 0.128.8 requires starlette<1.0.0,>=0.40.0.
fastapi==0.128.8
uvicorn[standard]==0.34.3
pydantic==2.9.2
pydantic-settings==2.3.4
email-validator==2.1.0
sqlalchemy==2.0.35
alembic==1.13.3
psycopg2-binary==2.9.9
# python-jose 3.3.0 had PYSEC-2024-232/233 (algorithm confusion + JWT decode bypass).
# Fixed in 3.4.0.
python-jose[cryptography]>=3.4.0
passlib[bcrypt]==1.7.4
# Pin for passlib 1.7.4 — bcrypt 4.1+ removed passlib's expected API and breaks hashing.
bcrypt==3.2.2
slowapi==0.1.9
prometheus-fastapi-instrumentator==7.1.0
# python-multipart <0.0.18 had CVE-2024-53981 (header injection) and later CVEs.
# Fixed in 0.0.27.
python-multipart>=0.0.27
python-docx>=1.1,<2.0
# pypdf <6.10.2 had multiple CVEs (path traversal, XML injection, ReDoS).
# Widened constraint to allow 6.x.
pypdf>=6.10.2
numpy==1.26.4
sentence-transformers>=2.2.0,<3.0
spacy>=3.7,<4.0
# scikit-learn was already implicitly required (used by features/tier10.py for TF-IDF
# fallback). Pinned explicitly for the Phase 2 topic resolver and Phase 5 adaptive
# weighting groundwork.
scikit-learn>=1.3,<2.0
# langdetect powers the Phase 2 language resolver (windowed code-switching detection).
# Pure Python, MIT-licensed, no transitive heavy deps.
langdetect>=1.0.9,<2.0
httpx==0.27.2
# starlette <0.40.0 had CVE-2024-47874 (HTTP request splitting). Fixed in 0.40.0.
# starlette <0.47.2 had CVE-2025-54121. Use >=0.47.2.
starlette>=0.47.2
# pytest-asyncio (all versions through 1.2.0) caps at pytest<9.
# Pin to >=8.2,<9 to stay compatible.
pytest>=8.2,<9
pytest-asyncio==0.24.0
pytest-cov==5.0.0
hypothesis==6.112.2
# black <26.3.1 had CVE-2026-32274. Dev-only but pinned for CI reproducibility.
black>=26.3.1
ruff==0.6.9
mypy==1.11.2
