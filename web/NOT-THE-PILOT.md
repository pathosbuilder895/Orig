# ⚠️ Abandoned — superseded by demo/

This React/TSX app was an earlier UI attempt. It is not served anywhere, not
maintained, and not part of the pilot. The live UI is `demo/` + `demo/bluebook/`.

See `docs/ARCHITECTURE.md`. Candidate for deletion once any useful styling
has been harvested.
