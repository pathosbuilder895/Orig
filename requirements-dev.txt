# requirements-dev.txt — dev-only dependencies.
#
# These are needed for tests, linting, the accuracy benchmarks
# (validation/benchmark/, validation/wide/, validation/public_authors/), and
# the documentation pipeline. They are NOT needed at runtime — the production
# deploy on Render uses requirements-pilot.txt (and the legacy demo uses
# requirements-demo.txt), neither of which depend on these.
#
# Install once:
#     pip install -r requirements-dev.txt

# Testing.
# pytest-asyncio (all versions through 1.2.0) caps at pytest<9. Pin to
# >=8.2,<9 to stay compatible.
pytest>=8.2,<9
pytest-asyncio==1.4.0
pytest-cov==7.1.0
hypothesis==6.167.1

# Lint/format/type-check + git hook runner.
ruff==0.6.9
mypy==2.3.0
pre-commit>=4.6.1,<5

# Plotting (ROC + calibration SVGs in the benchmark report).
matplotlib>=3.7,<4

# Used by validation.benchmark.metrics for some edge-case sanity checks
# (sklearn's calibration_curve / brier_score_loss are the canonical
# reference; we re-implement them in pure NumPy to keep them auditable,
# but having sklearn available means we can cross-check.)
# <1.7 matches the runtime pin (AI-likelihood artifact compat — see requirements.txt).
scikit-learn>=1.3,<1.7

# Mutation testing (T-31, docs/testing/02-unit-property-math.md §5). Pinned to
# the current release (3.7.0, 2026-09-08) — mutmut's 3.x line has a different
# execution/config model than 2.x (see [tool.mutmut] in pyproject.toml and
# docs/testing/mutation-baseline.md for the setup notes). Weekly, non-blocking;
# not part of the pytest/CI dependency graph.
mutmut==3.7.0

# pypdf + python-docx are already pinned in requirements.txt (Bbook needs
# them at runtime), so they are not duplicated here.
